# redm calculator
Used for a redm server to calc the items
mainly for learning tho so if anything is broke i wont fix it.

# Universal run calc (short for calculator)
can be used to calculate the amount of items needed for each run and the payout
supports upto 3 different items for a run might add more but fancy making it in imgui or something

# Universal run calc (UniCalc2)
same as the first 1 but different structure of code

# Universal Run Calc (UniCalc3)
same as the last but using different method of calculating instead of picking 1-3 items
user just puts in item at the start and can than put in weight and how many to make one sell price is after they all have finished

# Read Process Memory template (OpenProcessMemTemplate)
used as a template for when i want to read process memory of a application mainly single player games can be used with write process memory
to give money, xp, or whatever ingame
